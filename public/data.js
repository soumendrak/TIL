/* AUTO-GENERATED by scripts/build.mjs from posts/*.md — do not edit.
   Run "pnpm build" to regenerate, then commit this file with your post. */
window.TILS = [
  {
    "slug": "kintsugi-observability",
    "title": "Kintsugi — the gold in the crack, and why it fits observability",
    "date": "2026-07-04",
    "read": 2,
    "tags": [
      "philosophy",
      "observability",
      "mental-models"
    ],
    "preview": "Kintsugi repairs broken pottery with visible gold seams — not to hide damage, but to make it the most beautiful part. Observability does the same: illuminate failures, don't bury them.",
    "content": "<p><img src=\"./assets/kintsugi-sample.jpg\" alt=\"Kintsugi repaired pottery — gold seams illuminating the cracks\">\n<em>Kintsugi repair on a ceramic bowl. Photo by <a href=\"https://unsplash.com/@kittiko?utm_source=til&utm_medium=referral\">Riho Kitagawa</a> on <a href=\"https://unsplash.com/photos/JuDPjcutors?utm_source=til&utm_medium=referral\">Unsplash</a></em></p>\n<p>Kintsugi (金継ぎ, &quot;golden joinery&quot;) is the Japanese art of repairing broken pottery with urushi lacquer dusted with powdered gold. The crack is not hidden — it is highlighted with gold. The repair is physically stronger than the original clay, and the unique crack pattern makes the object irreplaceable.</p>\n<p>The deeper insight: hiding a crack requires a permanent lie. Displaying it frees the object from pretending. A visibly repaired bowl gets used daily; a &quot;perfect&quot; one gets wrapped and shelved.</p>\n<h2>The observability parallel</h2>\n<p>An AI system fails in production. Two paths:</p>\n<table>\n<thead>\n<tr>\n<th>Hide the crack</th>\n<th>Fill with gold</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Patch the symptom, no trace</td>\n<td>Capture the full trace</td>\n</tr>\n<tr>\n<td>No postmortem</td>\n<td>Write a postmortem</td>\n</tr>\n<tr>\n<td>Repeat incident next week</td>\n<td>Root cause identified</td>\n</tr>\n<tr>\n<td>System stays fragile</td>\n<td>System is stronger at the repaired point</td>\n</tr>\n</tbody></table>\n<p>This is the structural parallel. Observability is Kintsugi for software — making failures visible, learning from them, and building systems that are more resilient because they&#39;ve been broken and repaired with care.</p>\n<p>The same applies to careers. An unbroken facade creates hidden fragility. A narrative that shows the breaks and the repair has nothing to hide.</p>"
  },
  {
    "slug": "scroll-to-text-fragment",
    "title": "Scroll to Text Fragment — link to specific words on any page",
    "date": "2026-07-04",
    "read": 2,
    "tags": [
      "web",
      "html",
      "browsers"
    ],
    "preview": "You can link directly to any specific word on a webpage — no anchor tags needed. Just add",
    "content": "<h2>The trick</h2>\n<p>Add <code>#:~:text=YOUR_TEXT</code> to the end of any URL. When someone opens the link in Chrome, Edge, Brave, or Opera, the browser scrolls straight to that text and highlights it in yellow — no HTML anchors required.</p>\n<p>For example, this link jumps straight to &quot;AI Observability&quot; on my About page:\n<a href=\"https://soumendrak.com/about/#:~:text=AI%20Observability\"><code>soumendrak.com/about/#:~:text=AI%20Observability</code></a></p>\n<h2>Why it matters</h2>\n<p>Most pages don&#39;t put anchors on every paragraph. If you want to send someone to an exact quote or data point, text fragments let you do it without the page author&#39;s help — the browser does the work.</p>\n<h2>Context matching</h2>\n<p>If the phrase appears in multiple places, narrow it with prefix/suffix syntax:</p>\n<pre><code>#:~:text=prefix-,target,-suffix\n</code></pre>\n<p>Example:\n<code>#:~:text=making%20AI%20systems-,observable</code></p>\n<p>This matches &quot;observable&quot; only when it comes right after &quot;making AI systems&quot;.</p>\n<h2>Browser support</h2>\n<ul>\n<li>Chromium (Chrome 80+, Edge 80+, Brave, Opera) — full support with automatic yellow highlight</li>\n<li>Safari / Firefox — silently fall back to the top of the page (the link still works, just no scroll or highlight)</li>\n<li>The highlight fades after a few seconds</li>\n</ul>\n<h2>Source</h2>\n<p>Learned from <a href=\"https://mvark.blogspot.com/2026/07/how-to-link-directly-to-specific-word.html\">mvark.blogspot.com (TIL repo)</a>.</p>"
  },
  {
    "slug": "installing-codex-windows",
    "title": "Installing Codex CLI on Windows",
    "date": "2026-07-02",
    "read": 1,
    "tags": [
      "cli",
      "windows",
      "tools"
    ],
    "preview": "The best way to install OpenAI Codex on Windows is pnpm install -g @openai/codex. And the equivalent of claude --dangerously-skip-permissions is codex -a never.",
    "content": "<p>The <code>claude</code> CLI has <code>--dangerously-skip-permissions</code> for auto-yes mode. Codex does the same with:</p>\n<pre><code class=\"language-bash\">codex -a never\n</code></pre>\n<p>Where <code>-a</code> is short for <code>--auto-approve</code> and <code>never</code> means never ask for permission to run any command.</p>\n<p>Best way to install it on Windows:</p>\n<pre><code class=\"language-bash\">pnpm install -g @openai/codex\n</code></pre>\n<p>Then you&#39;re good to go.</p>"
  },
  {
    "slug": "brainwashing-vs-influence",
    "title": "Brainwashing removes agency, influence preserves it",
    "date": "2026-06-28",
    "read": 1,
    "tags": [
      "psychology",
      "ethics"
    ],
    "preview": "Brainwashing makes you obedient and afraid to question. Influence gives you better tools while keeping your right to disagree, verify and leave.",
    "content": "<table>\n<thead>\n<tr>\n<th>Test</th>\n<th>Healthy influence</th>\n<th>Brainwashing</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Questioning</td>\n<td>Doubts are allowed. Example: “Read Ambedkar and also read critics.”</td>\n<td>Doubts are punished. Example: “If you question this, your faith is weak.”</td>\n</tr>\n<tr>\n<td>Agency</td>\n<td>You choose what to accept. Example: “This teaching helps me set boundaries.”</td>\n<td>The group chooses for you. Example: “Obey the teacher before your own judgment.”</td>\n</tr>\n<tr>\n<td>Evidence</td>\n<td>Claims can be checked. Example: “Show the source, context and counterargument.”</td>\n<td>Claims are shielded. Example: “The guru said it, so it is true.”</td>\n</tr>\n<tr>\n<td>Relationships</td>\n<td>Old relationships are evaluated case by case. Example: “Keep family ties, but add boundaries.”</td>\n<td>Old relationships are treated as threats. Example: “Leave anyone who doubts the path.”</td>\n</tr>\n<tr>\n<td>Money and time</td>\n<td>Giving is voluntary and bounded. Example: “I can donate this much.”</td>\n<td>Giving proves loyalty. Example: “Sacrifice more or you are selfish.”</td>\n</tr>\n<tr>\n<td>Emotion</td>\n<td>You become calmer and more responsible. Example: “Delay anger, then act with evidence.”</td>\n<td>You become guilty and dependent. Example: “Feeling unsafe means your ego is resisting.”</td>\n</tr>\n<tr>\n<td>Exit right</td>\n<td>You can leave without fear. Example: “This is not for me anymore.”</td>\n<td>Leaving is framed as betrayal. Example: “If you leave, your life will collapse.”</td>\n</tr>\n<tr>\n<td>Net effect</td>\n<td>More clarity, dignity and freedom.</td>\n<td>More obedience, fear and isolation.</td>\n</tr>\n</tbody></table>"
  },
  {
    "slug": "smartphone-llm-interview-cheating",
    "title": "Smartphone LLM cheating in live coding interviews leaves visible signals",
    "date": "2026-06-23",
    "read": 1,
    "tags": [
      "llm",
      "interview",
      "hiring"
    ],
    "preview": "Signals Background blur was enabled. They looked at more than two places: keyboard, screen and another point. The shared screen did not change, but face brightness changed when their shoulder moved. They repeated or simplified the question multiple times. They used filler words…",
    "content": "<h2>Signals</h2>\n<ul>\n<li>Background blur was enabled.</li>\n<li>They looked at more than two places: keyboard, screen and another point.</li>\n<li>The shared screen did not change, but face brightness changed when their shoulder moved.</li>\n<li>They repeated or simplified the question multiple times.</li>\n<li>They used filler words until the app or LLM gave the answer.</li>\n<li>They gave gibberish answers at first, then suddenly gave polished answers.</li>\n</ul>"
  },
  {
    "slug": "agent-hooks",
    "title": "Agent hooks are lifecycle triggers for AI agents",
    "date": "2026-06-21",
    "read": 2,
    "tags": [
      "llm",
      "observability"
    ],
    "preview": "Agent hooks run custom logic when lifecycle events happen inside an AI agent. They are broader than decorators or context managers because they can react across the whole agent run.",
    "content": "<p>An AI agent hook is a trigger for agent events. When something happens inside the agent, such as an LLM call, tool call, error or run completion, the hook automatically runs custom logic.</p>\n<p>In simple terms:</p>\n<pre><code class=\"language-text\">event happens -&gt; hook runs\n</code></pre>\n<h2>Why hooks matter</h2>\n<p>Hooks let you attach cross-cutting behavior without putting that behavior inside every agent step.</p>\n<p>Common uses:</p>\n<ul>\n<li><strong>Observability:</strong> record spans, token counts, tool inputs, tool outputs and latency.</li>\n<li><strong>Security:</strong> block unsafe tools, redact secrets or flag suspicious prompts.</li>\n<li><strong>Evaluation:</strong> score a final answer, capture failed runs or sample traces for review.</li>\n<li><strong>Control:</strong> stop a run after too many retries, too much spend or a repeated failure loop.</li>\n</ul>\n<p>Without hooks, this logic gets scattered across prompts, tools and orchestration code. Then every new tool or agent loop has to remember to call the same telemetry, policy or cleanup code.</p>\n<h2>How hooks compare to familiar patterns</h2>\n<ul>\n<li><strong>Decorator:</strong> wraps one function.</li>\n<li><strong>Context manager:</strong> wraps one block.</li>\n<li><strong>Database trigger:</strong> reacts to one database event.</li>\n<li><strong>Agent hook:</strong> reacts to lifecycle events across the whole agent run.</li>\n</ul>\n<p>That broader lifecycle is the point. An agent is not one function call. It is a sequence of model calls, tool calls, decisions, retries and failures. Hooks give those moments named interception points.</p>\n<h2>The useful mental model</h2>\n<p>Treat hooks as the agent runtime&#39;s reflex system, not part of the agent&#39;s brain. The agent does the work, but hooks let the platform react automatically when something important happens.</p>\n<p>A better example is pulling your hand away from a hot pan. You do not explicitly plan the movement. The reflex arc fires before deliberate thought catches up.</p>\n<p>Hooks work the same way. The model should not decide whether telemetry is recorded, whether a policy check runs or whether cleanup happens after failure. The runtime should trigger those automatically.</p>"
  },
  {
    "slug": "navigator-sendbeacon",
    "title": "navigator.sendBeacon() — reliable data send on page unload",
    "date": "2026-06-13",
    "read": 2,
    "tags": [
      "web",
      "observability",
      "telemetry"
    ],
    "preview": "Before sendBeacon, sending telemetry on page close was unreliable — async fetch got cancelled, sync XHR blocked UX. sendBeacon guarantees the request goes through even on tab close. Essential for any web-based observability pipeline.",
    "content": "<h2>The problem (before)</h2>\n<p>You want to fire a telemetry ping when the user leaves the page — session duration, scroll depth, last interaction. All the old approaches had flaws:</p>\n<ul>\n<li><strong>Async fetch / XHR</strong> — browser cancels pending requests on unload. ~30% data loss in practice.</li>\n<li><strong>Sync XHR</strong> — blocks the unload. Users feel the lag. Lighthouse penalises it.</li>\n<li><strong>Image pixel hack</strong> — <code>&lt;img src=&quot;/track?data=...&quot;&gt;</code> — works sometimes, GET-only, ugly.</li>\n</ul>\n<pre><code class=\"language-js\">// Before — unreliable, data gets silently dropped\nwindow.addEventListener(&#39;unload&#39;, () =&gt; {\n  fetch(&#39;/api/track&#39;, { data: payload }); // cancelled by browser ⚠️\n});\n</code></pre>\n<h2>The fix (after)</h2>\n<p><code>navigator.sendBeacon()</code> queues the data and the browser commits to delivering it — even after the page is gone.</p>\n<pre><code class=\"language-js\">// After — browser guarantees delivery\ndocument.addEventListener(&#39;visibilitychange&#39;, () =&gt; {\n  if (document.visibilityState === &#39;hidden&#39;) {\n    navigator.sendBeacon(&#39;/api/track&#39;, JSON.stringify(payload));\n  }\n});\n</code></pre>\n<p>POST only, 64KB cap, fire-and-forget. Pair it with <code>visibilitychange</code> (not <code>unload</code>) for mobile coverage.</p>\n<h2>Why it matters for observability</h2>\n<p>If you&#39;re building web-based instrumention — agent dashboards, trace viewers, user analytics — <code>sendBeacon</code> closes a blind spot. Telemetry sent during page teardown was the data you were most likely to lose, and it&#39;s often the most important: the crash, the rage quit, the last interaction before the user left.</p>\n<p>In agent observability specifically: if you have a web UI that displays agent traces, <code>sendBeacon</code> lets you flush user interaction telemetry (which traces they viewed, how long they spent debugging) without worrying about the browser killing the request when they navigate to the next page.</p>"
  },
  {
    "slug": "git-worktrees-agentic-coding",
    "title": "Git worktrees for agentic AI coding",
    "date": "2026-06-06",
    "read": 2,
    "tags": [
      "git",
      "agents",
      "workflow"
    ],
    "preview": "Git worktrees let you check out multiple branches at once in separate directories. Unlike switching branches in a single working tree, worktrees give each branch its own state — no stashing, no conflicts, no context loss. Essential for parallel agentic coding workflows.",
    "content": "<h2>Worktree vs branch — what&#39;s the difference?</h2>\n<ul>\n<li><strong>Branches</strong> are just pointers to commits. Switching branches changes files in-place in your single working directory.</li>\n<li><strong>Worktrees</strong> are actual parallel checkouts. Each lives in its own directory but shares <code>.git/objects</code> — no duplicate storage.</li>\n<li>Switching branches with <code>&lt;branches only&gt;</code> = stashing/committing dirty state + re-indexing by your editor/agent</li>\n<li>Switching between worktrees = just <code>cd ../other-worktree</code> — both stay live, both keep their dirty files</li>\n</ul>\n<h2>Why agents need worktrees</h2>\n<ul>\n<li><strong>No context switching</strong> — an agent reviewing PR #42 on one branch can keep its state while another agent works on <code>feat/new-thing</code> in a separate worktree</li>\n<li><strong>No interference</strong> — Agent A can <code>git add</code>, run tests, leave files dirty. Agent B is unaffected in its own worktree</li>\n<li><strong>Parallel generative coding</strong> — one agent generates code, another reviews it, a third runs benchmarks. All on different branches, same repo, same time</li>\n<li><strong>Safe experimentation</strong> — scratch branches in worktrees can be deleted without touching <code>main</code> or other active work</li>\n<li><strong>Zero re-indexing</strong> — each worktree&#39;s editor/agent session keeps its own file watcher and index. No &quot;reload workspace&quot; on branch switch</li>\n</ul>\n<h2>Quick start</h2>\n<pre><code class=\"language-bash\"># One clone, unlimited worktrees\ngit clone git@github.com:org/repo &amp;&amp; cd repo\n\ngit worktree add ../repo-review -b review/pr-42    # Review branch\ngit worktree add ../repo-feat -b feat/new-thing     # Feature branch\ngit worktree add ../repo-scratch                    # Detached HEAD scratch\n\n# Each is a full checkout in its own directory\ncd ../repo-review &amp;&amp; ls\ncd ../repo-feat  &amp;&amp; ls\n\n# Clean up\ngit worktree remove ../repo-review\ngit branch -d review/pr-42         # if merged\ngit worktree prune                 # clean metadata\n</code></pre>\n<h2>Watch &amp; learn</h2>\n<ul>\n<li><a href=\"https://www.youtube.com/watch?v=2uEqYw-N8uE\">The best and most unknown Git feature — by ThePrimeagen</a> — 8 min, practical demo of why worktrees are game-changing</li>\n<li><a href=\"https://www.youtube.com/watch?v=8vsRb2mTBA8\">learn git worktrees in under 5 minutes — by bashbunni</a> — 3 min, fast intro with real workflow</li>\n<li><a href=\"https://www.youtube.com/watch?v=s4BTvj1ZVLM\">Git Tutorial #39: how to use git worktree — by GitKraken</a> — 11 min, thorough walkthrough of every command</li>\n<li><a href=\"https://www.youtube.com/watch?v=O-aBwXN200s\">Git Worktrees Tutorial #4: Worktree-First Approach — by Net Ninja</a> — 8 min, how to build a worktree-first development habit</li>\n</ul>"
  },
  {
    "slug": "agents-delete-own-telemetry",
    "title": "AI agents can delete their own logs and traces",
    "date": "2026-05-31",
    "read": 2,
    "tags": [
      "observability",
      "otel",
      "llm"
    ],
    "preview": "Autonomous agents with shell or filesystem access can wipe their own telemetry before you detect the bad behavior. Real-time observability plus sandboxing is a hard requirement, not a nice-to-have.",
    "content": "<p>OpenTelemetry just graduated from CNCF, second only to Kubernetes in contribution velocity. The conversation is shifting from &quot;how do we trace agents&quot; to &quot;what happens when the agent turns on us.&quot;</p>\n<p>The sharp risk: <strong>agents with shell or filesystem access can autonomously delete their own logs and traces.</strong> If your agent starts behaving badly (hallucinated tool calls, unauthorized data access, prompt injection), it can also run <code>rm -rf /var/log/*</code> or drop its own spans before they reach the collector. By the time you notice the damage, the evidence is gone.</p>\n<h2>What this means for observability</h2>\n<ul>\n<li><strong>Real-time export is non-negotiable.</strong> Batch-and-flush pipelines create a window where spans live only in the agent&#39;s memory or local disk. A compromised agent can drop them before export.</li>\n<li><strong>Sandbox the telemetry path.</strong> The agent process should not have write access to the OTel Collector socket or log directory. Telemetry egress must be at the infrastructure layer, not inside the agent&#39;s reach.</li>\n<li><strong>Watch for disappearing spans.</strong> If an agent&#39;s trace count drops suddenly while its activity level stays the same, that is itself a signal. Self-observability of the pipeline catches gaps the agent created intentionally.</li>\n</ul>\n<p>OpenLLMetry and OpenInference are adding model identity, prompt data and inference context to OTel spans over the next 6-12 months. That metadata is valuable. But it is only valuable if the spans actually reach the backend.</p>"
  },
  {
    "slug": "agent-hallucination-vs-llm",
    "title": "Agent hallucination vs traditional LLM hallucination",
    "date": "2026-05-20",
    "read": 2,
    "tags": [
      "llm",
      "observability"
    ],
    "preview": "Agent confidently says it used a tool and the answer is backed by that tool call, but never actually called the tool. It hallucinated the tool call itself. Traditional LLM hallucination is making up facts. Agent hallucination is making up actions.",
    "content": "<p>The agent tells you: <em>I called the search API and the result confirms my answer</em>. But it never called the API. It fabricated the tool call entirely.</p>\n<p>This is different from traditional LLM hallucination where the model makes up facts in its output. Here the model makes up <strong>actions</strong> — it hallucinates having executed a tool that it never touched.</p>\n<p>In observability terms: detecting a fake tool call in a trace (span exists but no actual API was hit) requires different instrumentation than detecting a factually wrong response. You need to verify the side effects, not just the output.</p>"
  }
];
